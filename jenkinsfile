pipeline {
    agent any
        options {
    ansiColor('xterm')
  }
    stages {
        stage('Back-end') {
            agent {
                docker { image 'rafaelsuzano/cypress:latest' }
            }
            steps {
                sh 'sudo npm install --unsafe-perm=true --allow-root'
            }
        }
  
    }
}
