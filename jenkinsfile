pipeline {
    agent any
        options {
    ansiColor('xterm')
  }
    stages {
        stage('Test') {
            agent {
                docker {
                    image 'rafaelsuzano/cypress:latest'
                   
                    reuseNode true
                }
            }
            steps {
                 
                     
            sh 'snpx cypress install --force --unsafe-perm=true --allow-root'
                } 
                 } 
             
             
       
    }
}
