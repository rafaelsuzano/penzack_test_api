pipeline {
    agent any
        options {
    ansiColor('xterm')
  }
    stages {
        stage('Back-end') {
            agent {
                docker { image 'rafaelsuzano/cypress:latest' }
            }
            steps {
                sh 'npx cypress run'
            }
        }
  
    }
}
