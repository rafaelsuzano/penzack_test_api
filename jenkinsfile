pipeline {
    agent any
        options {
    ansiColor('xterm')
  }
    stages {
        stage('Test') {
            agent {
                docker {
                    image 'rafaelsuzano/cypress:latest'
                   
                    reuseNode true
                }
            }
            steps {
                  {
                       sh 'npx cypress install --force'
                } 
                 } 
             
             
       
    }
}
